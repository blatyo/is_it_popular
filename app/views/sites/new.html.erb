<%= form_for @site, :url => sites_path do |f| %>
  <p id="label"><%= f.label :site, "Enter a site" %></p>
  <p id="input">
    <%= f.text_field :url %>
    <%= f.submit "Go", :id => 'site_submit' %>
  </p>
  <p id="example">
    e.g. www.google.com
  </p>
<% end %>
